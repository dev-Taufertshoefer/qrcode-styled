
exports.deriveOptimalQRDimsForStypling = function deriveOptimalQRDimsForStypling(opts) {

    if (opts.moduleStyle.type === 'rounded') {
        opts.scale = 10;
    }


    return opts
}


exports.insertImage = function insertImage (imgData, imageBufferToInsert) {
    
    imgData = imageBufferToInsert

    return imgData
}